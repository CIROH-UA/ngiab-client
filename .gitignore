# Python
*.pydevproject
*.project
*.egg-info
*.class
*.pyo
*.pyc
*.db
.pytest_cache
.venv

# Vi
*.swp

# System
*.DS_Store


# Tethys
tethysapp/*/workspaces/*
home/*

# Misc
*.sqlite
*.idea
*.vscode
*.coverage
*.swp
condorkey
condorkey.pub
tethysapp/ngiab/workspaces/*
services.yml
dist/*
build/*

# PDM
.pdm-python

# Node/React
node_modules*
# webpack output for the legacy React app (deleted at the Phase 2 cutover).
# NOTE: tethysapp/ngiab/public/frontend/ is hand-authored vanilla-JS source and IS tracked.
coverage


# singularity

.*sif

.claude

# Claude / compound-engineering working docs (kept local, not versioned)
docs/brainstorms/
docs/plans/
docs/solutions/
docs/superpowers/
# The design-system context the assistant reads. Stays at the repo root because the tooling
# looks for it there, but it is working context rather than part of the app.
DESIGN.md

# Assistant working notes: review findings, run artifacts, scratch analysis. Useful while
# the work is happening, noise in the history afterwards, and none of it is the codebase.
.context/